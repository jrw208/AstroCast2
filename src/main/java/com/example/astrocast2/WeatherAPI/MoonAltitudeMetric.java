package com.example.astrocast2.WeatherAPI;

/** Note: we didn't discuss this, but the altitude of the moon does significantly affect stargazing capability (as much as moon phase). 
 * Could ignore this if not necessary.
*/
public interface MoonAltitudeMetric extends WeatherMetric { }
